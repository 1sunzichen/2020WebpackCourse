import Ajv from 'Ajv';
console.log('====================================');
console.log('test.js文件被加载了~');
console.log('====================================');
export function mul(x, y) {
  return x * y;
}
// eslint-disable-next-line
export function count(x, y) {
  return x - y;
}
